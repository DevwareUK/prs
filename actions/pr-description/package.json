{
  "name": "@ai-actions/pr-description-action",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp src/*.js dist/"
  },
  "dependencies": {
    "@ai-actions/contracts": "workspace:*",
    "@ai-actions/core": "workspace:*",
    "@ai-actions/providers": "workspace:*"
  }
}

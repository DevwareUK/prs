{
  "name": "@prs/cli",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "prs": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --out-dir dist"
  },
  "dependencies": {
    "@prs/contracts": "workspace:*",
    "@prs/core": "workspace:*",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
